version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      # Bundle action SHA bumps into a single weekly PR so it's
      # one review instead of one PR per action.
      gha-bumps:
        patterns:
          - "*"
